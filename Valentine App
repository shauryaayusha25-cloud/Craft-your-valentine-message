# Initialize git (if not already done)
git init

# Add all files
git add .

# Commit
git commit -m "Initial commit - Valentine app"

# Add your GitHub repo as remote (replace with YOUR username and repo name)
git remote add origin https://github.com/YOUR-USERNAME/valentine-app.git

# Push to GitHub
git branch -M main
git push -u origin main
